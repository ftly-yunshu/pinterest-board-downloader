# Pinterest Board Downloader v3.1 - Python 依赖
# Last Tested: 2026-04-20
# 测试环境: Python 3.9 / 3.11 / 3.12 | macOS / Linux / Windows
#
# 最低版本要求（宽松，适合大多数用户）：
playwright>=1.40.0
aiohttp>=3.9.0
#
# 如果要锁定精确版本（生产/复现用），使用以下命令生成锁文件：
#   pip install playwright==1.44.0 aiohttp==3.9.5
#   pip freeze > requirements.lock.txt
#
# 安装依赖：
#   pip install -r requirements.txt
#   playwright install chromium
